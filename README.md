# I am learning opengl building simple app with SDL

# setup to run this project
installing system dependency 
gcc, g++, mingw 

add dependency library into vendor 
1. glm
clone glm repo and put glm folder inside vendor dir

2. SDL3 
clone and build sld3 for your system and if want cross-platform build for windows add sdl mingw source code from github into vendor dir

3. imgui
clone imgui repo into vendor dir to use 